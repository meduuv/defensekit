[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "defensekit"
version = "0.1.0"
description = "Defensive security data utilities"
requires-python = ">=3.9"
[tool.setuptools]
py-modules = ["defensekit"]
